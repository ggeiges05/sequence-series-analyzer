# sequence-series-analyzer
Python program that takes either a dataset or a sequence formula, figures out the pattern (arithmetic, geometric, or polynomial), finds the equation when possible, and calculates values like the nth term and partial sums in a reliable way.
